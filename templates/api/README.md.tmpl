# {{ .AppName }}

> 本 README 是业务项目说明占位。开始真实业务开发后，请把本文件替换为业务背景、领域模型、接口边界、部署方式和排障手册。

## 业务概览

`{{ .AppName }}` 是一个面向内部系统的 RESTful API 服务模板。当前代码提供认证、用户、文件上传和任务发布示例，用来占位展示常见业务能力的组织方式；真实项目应按业务域替换或裁剪这些示例模块。

建议在正式开发前补充以下内容：

- 服务负责的业务边界、上游系统和下游系统。
- 核心用户角色、关键业务流程和主要接口清单。
- 数据归属、权限模型、审计要求和合规要求。
- 本地开发、测试、预发、生产环境的差异。

## 当前模块

```text
cmd/server                 服务启动入口
configs/                   分环境配置
db/migrations              Atlas 版本化迁移
db/seeds                   初始化数据
internal/boot              启动、配置、依赖装配和生命周期
internal/data/schema       Ent schema，数据库结构主源
internal/modules/auth       登录、刷新 token 和登出示例
internal/modules/user       用户查询与当前用户信息示例
internal/modules/file       local 文件上传、下载和删除示例
internal/modules/taskdemo   异步任务发布示例
```

示例模块用于展示工程结构，不代表真实业务命名。新增业务时优先创建独立模块；不需要的示例模块应在替换业务后删除，并同步清理路由、权限策略、迁移和种子数据。

## 快速启动

```powershell
docker compose up -d postgres redis
initra migrate apply --env local
psql "postgresql://postgres:postgres@127.0.0.1:5432/initra?sslmode=disable" -f db/seeds/001_seed_admin.sql
$env:APP_ENV = "local"
go run ./cmd/server
```

默认账号仅用于本地验证：

- 用户名：`admin`
- 密码：`admin123`

首次接入真实环境时，请替换默认密码、JWT 密钥、数据库连接、Redis 连接和对象存储配置。

## 配置

配置加载顺序：

1. `configs/config.yaml`
2. `configs/config.<env>.yaml`
3. 环境变量覆盖

运行环境由 `--env`、`APP_ENV` 或默认值 `dev` 决定；其他环境变量默认使用 `INITRA_` 前缀，例如 `INITRA_AUTH_JWT_SECRET`、`INITRA_DATABASE_DSN`。

常用配置分组：

- `app`、`server`、`database`、`redis`、`auth`、`log`、`observability`：基础运行配置。
- `casbin`：权限模型和策略文件。
- `cache`、`idgen`、`storage`、`task`：缓存、雪花 ID、文件存储和任务队列配置。

配置打印前会走脱敏逻辑。密码、Token、Secret、Access Key、Authorization 和带密码的 DSN 不应明文进入日志。

## 数据库与迁移

`internal/data/schema` 是数据库结构主源；`db/migrations` 是版本化迁移历史；`db/seeds` 保存初始化数据。

```powershell
go generate ./internal/data
initra migrate diff <name> --env local
initra migrate apply --env local
initra migrate hash
```

修改 Ent schema 后先执行 `go generate ./internal/data`，再生成迁移。迁移 diff 必须使用 `initra migrate diff <name>` 或 `go run ./internal/data/migratediff/main.go <name>`；手动修改迁移文件后使用 `initra migrate hash` 重新计算 `atlas.sum`。

`db/schema/*.sql` 仅作为历史参考或 DBA 阅读材料，不再作为 Atlas diff 的 schema source。

## 开发与验证

```powershell
go test ./... -count=1
go vet ./...
go build ./cmd/server
```

新增业务模块时同步补充单元测试；涉及数据库读写时优先补充 repository 或集成测试；涉及路由权限时同步更新 Casbin policy 和路由安全声明。

## 文档替换清单

正式业务开发前，建议至少替换这些占位内容：

- 本服务的业务名称、业务目标和接口范围。
- 默认账号、示例模块说明和示例接口。
- 本地依赖启动方式、外部服务依赖和环境变量清单。
- 数据库初始化、迁移发布和回滚流程。
- 告警、日志字段、排障入口和常见问题。
