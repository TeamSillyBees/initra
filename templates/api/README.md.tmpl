# {{ .AppName }}

本项目是基于 initra 生成的 Go RESTful API 服务。它提供一套可运行的内部系统起点：Huma/Gin Web 层、配置加载、认证鉴权、Ent/Atlas 迁移、日志、Redis、缓存、文件存储、HTTP Client、任务队列和测试骨架。

当前代码包含 `auth`、`user`、`file`、`httpdemo`、`taskdemo` 示例模块，用来展示框架能力的正确接入方式。正式业务开发时应按业务域替换或裁剪示例模块，不要把示例接口、默认账号和本地密钥带入共享环境。

## 目录结构

```text
cmd/server                  服务启动入口
configs/                    分环境配置、Casbin model 和 policy
db/migrations               Atlas 版本化迁移
db/seeds                    初始化数据
internal/boot               启动、配置、依赖装配和生命周期
internal/data/schema        Ent schema，数据库结构主源
internal/modules/auth       登录、刷新 token、当前用户示例
internal/modules/user       用户查询与管理示例
internal/modules/file       local 文件上传、下载、元信息和删除示例
internal/modules/httpdemo   命名 HTTP Client 调用示例
internal/modules/taskdemo   异步任务发布示例
internal/modules/bizerrors  业务错误码和错误工厂
test/                       架构、集成和 e2e 测试
```

业务代码只应依赖 `github.com/teamsillybees/initra/pkg/*` 提供的框架能力，禁止 import `github.com/teamsillybees/initra/internal/*`，也不要复制 initra 根仓库的 `pkg/*` 到本项目。

## 首次处理

```powershell
initra skill
```

该命令会写入 `.agents/skills/initra-framework`，让 Codex 在本项目内按 initra 的框架边界、模块结构和 package 最佳实践工作。使用 Claude Code 时执行 `initra skill cc`。

正式接入业务前，至少完成这些替换：

- 修改服务名称、配置中的密钥、数据库连接、Redis 连接、对象存储配置和默认管理员密码。
- 删除不需要的示例模块，并同步清理 `internal/boot/providers.go`、路由、Casbin policy、测试、迁移和种子数据。
- 根据真实业务角色补齐 Casbin `Resource`、`Action` 和策略文件。
- 把本 README 改成真实业务说明：业务边界、上游下游、接口清单、部署方式、告警和排障入口。

## 快速启动

```powershell
docker compose up -d postgres redis
initra migrate apply --env local
psql "postgresql://postgres:postgres@127.0.0.1:5432/initra?sslmode=disable" -f db/seeds/001_seed_admin.sql
$env:APP_ENV = "local"
go run ./cmd/server
```

默认账号仅用于本地验证：

- 用户名：`admin`
- 密码：`admin123`

常用验证命令：

```powershell
go test ./... -count=1
go vet ./...
go build ./cmd/server
```

## 配置

配置加载优先级：

1. 环境变量
2. `configs/config.<env>.yaml`（存在时）
3. `configs/config.yaml`（存在时）
4. `internal/boot/config.go` 中的默认值

服务进程的运行环境由 `APP_ENV` 决定，未设置时使用默认值 `dev`；`initra migrate` 等 CLI 命令可通过 `--env` 显式指定环境。其他环境变量默认使用 `INITRA_` 前缀，例如 `INITRA_AUTH_JWT_SECRET`、`INITRA_DATABASE_DSN`。

常用配置分组：

- `app`、`server`、`database`、`redis`、`auth`、`log`、`observability`：基础运行配置。
- `casbin`：权限模型和策略文件。
- `cache`、`idgen`、`storage`、`task`、`http_client`：缓存、雪花 ID、文件存储、任务队列和命名 HTTP Client。

配置打印前会走脱敏逻辑。密码、Token、Secret、Access Key、Authorization、Cookie、验证码、session value、完整请求体、完整响应体和带密码的 DSN 不应明文进入日志。

业务 ID 统一使用 `github.com/teamsillybees/initra/pkg/idgen.ID`。Ent 主键、外键、auth user ID、REST path params、service 入参和 JSON VO 不使用 `int64` 或 `string` 表达业务 ID；REST/OpenAPI 对外暴露为 JSON string，示例形如 `"1771234567890123456"`。只有对接雪花 ID 生成器、第三方库、手写 SQL 参数或底层基础设施时才调用 `Int64()`。

## 模块开发

业务代码按业务模块组织为单一 flat package，不拆 controller/service/repository 子目录。标准结构：

```text
internal/modules/<module>/
  <module>.handler.go HTTP 适配方法，调用 service 并包装响应
  <module>.dto.go     HTTP 边界类型：Request/Response、Query、Body、VO
  <module>.service.go 业务逻辑 + Ent 数据库操作 + 必要的私有小接口
  <module>.routes.go  路由注册 + Module 结构体
  providers.go        samber/do 依赖注入
  cache.go            可选，缓存适配器或少量领域实体
  <module>_test.go    单元测试
```

开发约束：

- Handler 不定义 DTO；只做参数转换、调用 service 和响应包装。
- Service 直接接受 HTTP 边界 `Body`/`Query` 类型并返回 `VO`，不再定义 `CreateXxxDTO`、`UpdateXxxDTO` 这类 service DTO。
- Service 直接通过 Ent Client 操作数据库，不默认拆 Repository 层；跨模块调用优先依赖调用方内部定义的小接口。
- `*.dto.go` 中 Huma/Gin 包装类型用非导出的 `request`/`response` 后缀，查询参数用 `Query`，请求体用 `Body`，对外 JSON 类型用 `VO`。
- 新增 `/api/` 路由必须通过 `registry.Register` 登记 `RouteSecurity`；公开接口用 `AccessModePublic`，登录即可访问接口用 `AccessModeAuthenticated`，后台或运营接口用 `AccessModePermission` 并同步 Casbin policy。

## 数据库与迁移

`internal/data/schema` 是数据库结构主源；`db/migrations` 是版本化迁移历史；`db/seeds` 保存初始化数据。Ent schema 复用 `pkg/entx/mixin` 中的 ID、审计、软删除和乐观锁 mixin，不在业务项目内复制本地 mixin。

```powershell
go generate ./internal/data
initra migrate diff <name> --env local
initra migrate apply --env local
initra migrate hash
```

修改 Ent schema 后先执行 `go generate ./internal/data`，再生成迁移。手动修改迁移文件后使用 `initra migrate hash` 重新计算 `atlas.sum`。

## 框架能力

- Redis：使用 `pkg/redisx` 的 client、Key Builder、Lua registry、SCAN+UNLINK 和 redislock；生产禁止使用 `KEYS`。
- 缓存：使用 `pkg/cache`，明确 key 命名、TTL 和失效策略。
- 文件存储：使用 `pkg/storage` 和 `pkg/storage/provider`，业务模块不要直接依赖云厂商 SDK。
- HTTP Client：由 `httpclient.Register(injector, cfg.HTTPClient)` 统一装配，业务模块通过命名 client 或最小接口依赖。
- 任务队列：使用 `pkg/task` 和 `pkg/task/asynqadapter` 的装配入口；业务代码不得直接 import `github.com/hibiken/asynq`。
- 依赖装配：框架能力包在 `internal/boot/providers.go` 中通过 `Register(injector, cfg)` 显式注册一次；业务模块只在自己的 `providers.go` 注册本地 handler、service、module。

## 开发与验证

窄改动至少运行相关包测试；影响配置、迁移、路由权限、依赖装配或共享模块时运行完整验证：

```powershell
go test ./... -count=1
go vet ./...
go build ./cmd/server
```

新增业务模块应同步补充单元测试，使用 fake 验证 service 编排逻辑；涉及数据库读写时补充集成测试或 `go-sqlmock` 验证 SQL、事务和参数；涉及架构边界时保留或补充测试，确保项目不 import initra 根仓库的 `internal/*`。
