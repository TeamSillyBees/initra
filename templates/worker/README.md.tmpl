# {{ .AppName }} worker

这是 `initra new <app> --type worker` 生成的后台任务项目骨架，默认演示 `pkg/task` 的 Asynq 适配能力：

- 注册 `demo:send_email` 任务处理器。
- 注册 `demo:cleanup_expired_sessions` 周期任务。
- 使用 `Publisher / Worker / Scheduler / Registry` 命名，不在业务代码中直接依赖 Asynq。

如果后台任务需要处理文件，优先复用根模块 `pkg/storage` 的统一文件与对象存储接口，通过配置切换 local、阿里云 OSS、腾讯云 COS、AWS S3 或 S3 兼容 provider，避免任务代码直接依赖云厂商 SDK。

## 常用命令

```powershell
$env:INITRA_TASK_REDIS_ADDR="127.0.0.1:6379"
$env:INITRA_TASK_REDIS_DB="2"
go test ./... -count=1
go vet ./...
go run ./cmd/worker
```
